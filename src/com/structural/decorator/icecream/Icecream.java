package com.structural.decorator.icecream;

/**
 * The Component declares the common interface for both wrappers(decorators) and wrapped objects(decorated objects).
 */
public interface Icecream {
    public String makeIcecream();
}
