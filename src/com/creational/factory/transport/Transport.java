package com.creational.factory.transport;

/**
 * The "Product" declares the interface,
 * which is common to all objects that can be produced by the creator and its subclasses.
 */
public interface Transport {
    void deliver();
}
